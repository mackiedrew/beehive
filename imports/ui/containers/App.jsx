// @flow

// Framework
import type React, { Element } from 'react';

/**
 * Main rendering entry point for the app.
 *
 * @returns {ReactElement} Stateless functional React component.
 */
const App = (): Element<*> => (
  <div className="app">
    Test 123
  </div>
);

export default App;
